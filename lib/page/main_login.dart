import 'package:firtsapp/main_login.dart';
import 'package:flutter/material.dart';

void main() => runApp( egapp());

class egapp extends StatelessWidget {

  @override
  Widget build(BuildContext context) {
    return MaterialApp(
     debugShowCheckedModeBanner: false,
     home: Pagelogin(),
    );
  }
}